const QUNATIZED_WIDTHS = [0, 36, 48, 56, 64];

export function quantizedWidth(width: number): number {
  // let result: number | null = null;
  // QUNATIZED_WIDTHS.every((qWidth) => {
  //   if (qWidth > width) {
  //     result = qWidth;
  //     return false;
  //   }
  //   return true;
  // });
  return width;
}
